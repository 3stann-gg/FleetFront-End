/** Table UI placeholder — no data loading */
(function () {
  function initTableUi() {
    // Reserved for future row selection helpers (no API)
  }
  document.addEventListener("DOMContentLoaded", initTableUi);
})();
