/**
 * Module starter boot file
 * Loads navigation UX only. No business logic / no API.
 */
document.addEventListener("DOMContentLoaded", () => {
  // navigation.js self-inits
  // Add module-specific init hooks here later
});
